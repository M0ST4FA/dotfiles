#!/bin/sh

if [ "$#" -ne 1 ]; then
  printf 'usage: %s EMAIL_ADDRESS\n' "${0##*/}" >&2
  exit 2
fi

exec secret-tool store \
  --label="NeoMutt password for $1" \
  application neomutt \
  account "$1"
